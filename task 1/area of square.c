//area of square
#include<stdio.h>
void main(){
    float side,area;
    printf("enter the side length:");
    scanf("%f",&side);
    area=side*side;
    printf("area of square is:%f",area);
}